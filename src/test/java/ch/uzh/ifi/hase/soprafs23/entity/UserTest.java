package ch.uzh.ifi.hase.soprafs23.entity;

import ch.uzh.ifi.hase.soprafs23.constant.UserStatus;
import org.junit.Assert;
import org.junit.Test;

import java.util.Date;

import static org.junit.jupiter.api.Assertions.assertEquals;

public class UserTest {
    /*
    @Test
    public void testConstructorAndGetter() {
        Long id = 1L;
        String password = "password";
        String username = "username";
        String token = "token";
        String email = "myMail";
        UserStatus userStatus = UserStatus.ONLINE;
        String bio = "myBio";
        Date date = new Date("05.05.2005");
        boolean host = false;
        boolean guest = false;

        User user = new User();
        user.setId(id);
        user.setPassword(password);
        user.setUsername(username);
        user.setToken(token);
        user.setEmail(email);
        user.setStatus(userStatus);
        user.setBio(bio);
        user.setCreation_date(date);
        user.setHost(host);
        user.set_guest_user(guest);

        Assert.assertEquals(id,user.getId());
        Assert.assertEquals(password,user.getPassword());
        Assert.assertEquals(username,user.getUsername());
        Assert.assertEquals(token, user.getToken());
        Assert.assertEquals(email, user.getToken());
        Assert.assertEquals(userStatus, user.getStatus());
        Assert.assertEquals(bio, user.getBio());
        Assert.assertEquals(date, user.getBio());
        Assert.assertEquals(host, user.isHost());
        Assert.assertEquals(guest, user.is_guest_user());

    }
    @Test
    void isTrue(){
        int one = 1;
        assertEquals(1,one);
    }*/
}
